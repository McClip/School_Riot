using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ESTOESGOD : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        // EL SEBAS ESTÁ FLIPANDO

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
